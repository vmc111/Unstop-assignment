// Tailwind v4 config (optional). Using JS because Tailwind picks this up without extra tooling.
// Matches your provided settings from tailwind.config.ts
export default {
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
  theme: {
    extend: {},
  },
  plugins: [],
}
